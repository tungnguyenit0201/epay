import { Spacings } from 'react-native-ui-lib';
import { Spacing } from 'themes';

Spacings.loadSpacings({
  page: Spacing.PADDING,
  middle: Spacing.PADDING * 0.75,
  card: Spacing.PADDING * 0.5
});
